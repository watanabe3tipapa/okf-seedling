---
title: "1. Create a Bundle"
description: quarto use template で新しい OKF バンドルを作成する手順
---

# 新しいバンドルを作成する

## 前提

- [Quarto](https://quarto.org/docs/get-started/) (>= 1.3) がインストール済み
- GitHub に公開済みのテンプレートリポジトリ

## 手順

```bash
quarto use template <your-github-org>/okf-seedling
```

空ディレクトリで実行すると、以下の成果物が生成されます。

- `_quarto.yml`(サイト・OKF 設定)
- `concepts/`(concept 型の雛形 .qmd)
- `tutorial/`(このドキュメント群)
- `tools/`(バンドル生成・検証・スキーマ生成スクリプト)
- `_extensions/okf/`(frontmatter → HTML 表示の Quarto 拡張)

## レンダリング

```bash
quarto render
```

- `_site/` … 人間用 HTML(LP・チュートリアル)
- `okf/` … OKF v0.2 準拠バンドル(.md + frontmatter、機械読み取り用)

## 検証

```bash
node tools/validate-okf.mjs
```

出力バンドルが OKF v0.2 に準拠しているか、type レジストリ(`tools/okf-types.json`)駆動で
必須 frontmatter・provenance・内部リンクの解決可能性をチェックします。
